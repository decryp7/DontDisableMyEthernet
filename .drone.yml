kind: pipeline
type: exec
name: default

platform:
  os: windows

steps:
- name: compile
  environment:
    msbuild_path:
      from_secret: msbuild_path
  commands:
  - Invoke-MsBuild -Path DontDisableMyEthernet.sln
